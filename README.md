## Tecnologias utilizadas

- React JS
- JavaScript (ES6+)
- Vite
- HTML5 / JSX
- CSS3
- Node.js / npm

## Pré-requisitos

- [Node.js](https://nodejs.org) (versão 18 ou superior)
- npm (já vem instalado junto com o Node.js)

Para verificar, rode no terminal:

```bash
node -v
npm -v
```

## Instalação

1. Acesse a pasta do projeto
```bash ou terminal
   cd react-jovinho
```

2. Instale as dependências
```bash ou terminal
   npm install
```

## Execução do projeto

Após instalar as dependências, rode o comando abaixo para iniciar o projeto em modo de desenvolvimento:

```bash ou terminal
npm run dev
```

O terminal vai exibir o endereço local onde o projeto está rodando, geralmente:
http://localhost:5173/
Basta abrir esse link no navegador.

Se você deseja abrir de outro jeito para o navegador faça assim:

```bash ou terminal
o + enter
```
Para abrir no browser

## Usuarios e senhas

- Email: "jovinho@fiap.com.br"
- Senha: Qualquer uma serve
- (Email e senha também visíveis no site)

## Uso de Inteligência Artificial

Durante o desenvolvimento deste projeto, foi utilizada IA (Claude, da Anthropic) como ferramenta de apoio nas seguintes etapas:

- Conversão de lógica de login originalmente escrita em JavaScript puro (manipulação de DOM) para o padrão de componentes React, utilizando hooks como `useState` e `useEffect`.
- Implementação de funcionalidades com o objeto `Math`, como cálculo de força de senha e geração de senha aleatória.
- Estruturação de um componente de avaliação por estrelas.
- Auxílio na escrita e organização deste README.

A lógica de negócio, validações e decisões de estrutura do projeto foram definidas e revisadas pelo desenvolvedor, com a IA atuando como suporte técnico e educacional durante o processo.